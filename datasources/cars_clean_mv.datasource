DESCRIPTION >
    Cleaned and deduplicated cars date

SCHEMA >
    `angle` Int32,
    `datetime` DateTime,
    `sensor` Int32,
    `ih` Int32,
    `point` String

ENGINE MergeTree
ENGINE_PARTITION_KEY toYYYYMM(datetime)
ENGINE_SORTING_KEY datetime, sensor, ih, point
