DESCRIPTION >
    The last data available for each sensor

NODE per_sensor
DESCRIPTION >
    Obtain the last measurement taken by each sensor

SQL >
    SELECT idpm, max(datetime), argMax(ih, datetime) from traffic group by idpm

NODE count_results
SQL >
    SELECT count() FROM per_sensor
